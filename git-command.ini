constructor need to be added if we use props or state
render will render script on the page

git add .
git status
git commit -m "message"
git push origin master
git pull origin master

parent declare method
sent the method by props to child
child call the method from parent

parent set props
child call props from parent